// * RUN with VS Code's "Live Server" extention and see results in browser's debugger
import { moduleFunction, foo } from './utils.js';

console.log('....', moduleFunction());
console.log('....', foo);